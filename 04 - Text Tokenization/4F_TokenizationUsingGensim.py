from gensim.utils import tokenize

# Create a string input
input_str = "Artificial intelligence is transforming the world"

# Tokenize the text
tokens = list(tokenize(input_str))

# Print the list of tokens
print(tokens)
